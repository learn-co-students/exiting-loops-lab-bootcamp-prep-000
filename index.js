function breakOut(array, changeValue, stopValue){
  for(let i = 0, l = array.length; i < l; i++){
    if (changeValue array[i] !==  Math.floor(Math.random() * 100)){
    break
    }
  return (array)
  }
}
